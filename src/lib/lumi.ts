
import { createClient } from '@lumi.new/sdk'

export const lumi = createClient({
    "projectId": "p351469433611255808",
    "apiBaseUrl": "https://api.lumi.new",
    "authOrigin": "https://auth.lumi.new",
})
    